@model TableViewModel

<div class="container-fluid">
    <div class="card shadow border-0 mt-4">
        <div class="card-header bg-secondary bg-gradient ml-0 py-2">
            <div class="row">
                <div class="col-12 text-center">
                    <h2 class="text-white py-2">@(Model.Table.Id != 0 ? "Update" : "Add") Table</h2>
                </div>
            </div>
        </div>
        <div class="card-body p-4">
            <form method="post" class="user" enctype="multipart/form-data">
                <input asp-for="Table.Id" hidden />
                <div class="row">
                    <div class="col-12">
                        <div class="border p-3 mt-4">
                            @*<div asp-validation-summary="All"></div>*@
                            <div class="form-group col-12">
                                <input asp-for="Table.Seats" class="form-control" />
                                <label asp-for="Table.Seats" class="ms-2"></label>
                                <span asp-validation-for="Table.Seats" class="text-bg-danger"></span>
                            </div>
                            <div class="form-group col-12">
                                <select asp-for="Table.RestaurantGroupId" asp-items="@Model.RestaurantGroupList" class="form-control">
                                    <option disabled selected>--Select Restaurant--</option>
                                </select>
                                <label asp-for="Table.RestaurantGroupId" class="ms-2"></label>
                                <span asp-validation-for="Table.RestaurantGroupId" class="text-bg-danger"></span>
                            </div>
                            <div class="row">
                                <div class="col-6 col-md-3">
                                    <button type="submit" class="btn btn-primary form-control">@(Model.Table.Id != 0 ? "Update" : "Add")</button>
                                </div>
                                <div class="col-6 col-md-3">
                                    <a asp-controller="Table" asp-action="Index" class="btn btn-outline-primary border form-control">
                                        Back To List
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>


@section Scripts{
    <partial name="_ValidationScriptsPartial" />
}